import 'package:flutter/material.dart';

class SuReports extends StatelessWidget {
  const SuReports({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
